# Berlim Sistema
Sistema de ERP Corporativo da Berlim Gráfica.

## Como rodar o projeto localmente:
1. Instale as dependências: `npm install`
2. Inicie o servidor: `npm run dev`
3. Acesse `http://localhost:3000`
